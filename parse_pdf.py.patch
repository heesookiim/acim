import re

def normalize_title(title):
    t = title.strip()
    t = re.sub(r'^(제?\d+장\s+|[IVXLC]+\.\s*|\d+\.\s*)', '', t)
    t = re.sub(r'\s+\d+$', '', t)
    t = re.sub(r'[\xa0\s]+', ' ', t).strip()
    return t
